#!/usr/bin/env sh
# Generated by wrapper
DIRNAME="$(cd "$(dirname "$0")" && pwd)"
# Use classpath run in case the jar has no Main-Class manifest entry
exec java -Dmaven.multiModuleProjectDirectory="$DIRNAME" -cp "$DIRNAME/.mvn/wrapper/maven-wrapper.jar" org.apache.maven.wrapper.MavenWrapperMain "$@"
